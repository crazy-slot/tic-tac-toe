export const ENVIRONMENT = process.env.NODE_ENV;

export const BUILD_ID: string = process.env.NEXT_PUBLIC_BUILD_ID || '';

export const BUILD_DATE: string = process.env.NEXT_PUBLIC_BUILD_DATE || '';

export const BUILD_SHA: string = process.env.NEXT_PUBLIC_BUILD_SHA || '';
